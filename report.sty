% +---------------------------------------------------------------+
% | Author :    Sylvain Pasini, HEIG-VD
% | Date :       February 15th, 2012
% +---------------------------------------------------------------+


% +---------------------------------------------------------------+
% | A few useful packages
% +---------------------------------------------------------------+
\usepackage{graphicx,color}

% to print url
\usepackage{url}

% Allow number computations
\usepackage{calc}

% Smileys
\usepackage{wasysym}
% exemples : \smiley or \frownie



% +---------------------------------------------------------------+
% | Layout
% +---------------------------------------------------------------+
\def\spirale{6mm} 
\def\demispirale{3mm}

%1 Offset horizontal
\setlength{\hoffset}{0pt}                            
%2 Offset vertical
%4 Top margin
\setlength{\topmargin}{6mm}
%5 head height
\setlength{\headheight}{12pt}
%6 head sep
\setlength{\headsep}{15mm}
%7 Text height
\setlength{\textheight}{200mm}
%8 Text width
\setlength{\textwidth}{\paperwidth - 2in - \spirale}
%9 Margin sep
\setlength{\marginparsep}{5pt}
%10 Margin notes width
\setlength{\marginparwidth}{60pt}
%11 Foot skip
\setlength{\footskip}{15mm+12pt}

\DeclareOption{reliure}%
    {\setlength{\oddsidemargin}{\spirale}%
     \setlength{\evensidemargin}{0mm}}
\DeclareOption{centre}%
     {\setlength{\oddsidemargin}{\demispirale}%
      \setlength{\evensidemargin}{\demispirale}}
% By default use the reliure
\ExecuteOptions{reliure}
% My options (process the specified options)
\ProcessOptions

% Package used to print layout data
\usepackage{layout}


% +---------------------------------------------------------------+
% | Bibliographies indépendantes pour chaque section
% +---------------------------------------------------------------+
\makeatletter
\def\thebibliography#1{\subsection{\bibname}\list
  {\@biblabel{\arabic{enumiv}}}{\settowidth\labelwidth{\@biblabel{#1}}%
    \leftmargin\labelwidth
    \advance\leftmargin\labelsep
    \usecounter{enumiv}%
    \let\p@enumiv\@empty
    \def\theenumiv{\arabic{enumiv}}}%
    \def\newblock{\hskip .11em plus.33em minus.07em}%
    \sloppy\clubpenalty4000\widowpenalty4000
    \sfcode`\.=\@m}
\makeatother


% +---------------------------------------------------------------+
% | Hyperref
% +---------------------------------------------------------------+
\usepackage{hyperref}
\hypersetup{
  pdftitle={Tweet Analyser}, %informations apparaissant dans
  pdfauthor={Dejvid MUAREMI, Mentor REKA, Xavier VAZ AFONSO}, %dans les informations du document
  pdfsubject={SCALA - Projet} %sous Acrobat.
}
